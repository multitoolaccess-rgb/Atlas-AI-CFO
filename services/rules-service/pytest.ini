[pytest]
# `pythonpath = .` injects services/rules-service/ into sys.path so the
# flat `from app.main import app` import used in tests/test_health.py
# resolves when pytest is invoked from any working directory. Equivalent
# to `python -m pytest` from inside services/rules-service.
pythonpath = .
testpaths = tests
