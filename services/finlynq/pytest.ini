[pytest]
# `pythonpath = .` injects services/finlynq/ into sys.path so the flat
# `from app.main import app` imports used in tests/* resolve when pytest
# is invoked from any working directory. Equivalent to running
# `python -m pytest` from inside services/finlynq.
pythonpath = .
testpaths = tests
