# services/finlynq/app/routes/__init__.py
#
# Phase F1 — only the health + parser/categorizer/state stubs are wired.
# Phase F3-F5 lifts drop in real route modules here. Imports are kept
# absolute (not relative to ``..``) to match rules-service's convention.
